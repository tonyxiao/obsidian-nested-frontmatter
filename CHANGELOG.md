# Changelog

## 0.1.3

- Track the JavaScript `main.js` source in git so GitHub releases include the actual plugin runtime
- Keep the no-build JavaScript-first authoring workflow and `checkJs` setup

## 0.1.2

- Fix JavaScript `checkJs` configuration for the no-build release flow
- Keep the JavaScript-first plugin source and existing behavior intact

## 0.1.1

- Switch plugin authoring from TypeScript plus bundling to checked JavaScript with no build step
- Keep the same structured frontmatter rendering and nested wikilink behavior

## 0.1.0

- Initial release
- Render nested object and array frontmatter values in the native Properties pane
- Support single-click navigation for nested wikilinks
